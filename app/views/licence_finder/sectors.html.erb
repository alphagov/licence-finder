
<%= form_tag sectors_path do %>
<ul>
  <% @sectors.each do |sector| %>
  <li>
    <%= check_box_tag 'sectors[]', sector.public_id, false, :id => "sector_#{sector.public_id}" %>
    <%= label_tag "sector_#{sector.public_id}", sector.name %>
  </li>
  <% end %>
</ul>
<% end %>
